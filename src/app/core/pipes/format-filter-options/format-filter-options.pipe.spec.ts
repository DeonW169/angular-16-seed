import { FormatFilterOptionsPipe } from './format-filter-options.pipe';

describe('FormatFilterOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFilterOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
